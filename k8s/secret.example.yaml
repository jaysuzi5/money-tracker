# Template only. DO NOT commit real values.
# Fill in, then seal with kubeseal before applying:
#   kubeseal --format yaml < secret.example.yaml > sealed-secret.yaml
#   kubectl apply -f sealed-secret.yaml
#
# Generate values:
#   SECRET_KEY:  python -c "import secrets;print(secrets.token_urlsafe(50))"
#   FERNET_KEY:  python -c "from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())"
apiVersion: v1
kind: Secret
metadata:
  name: money-tracker
  namespace: money-tracker
type: Opaque
stringData:
  SECRET_KEY: "REPLACE_ME"
  FERNET_KEY: "REPLACE_ME"
  POSTGRES_USER: "jcurtis"
  POSTGRES_PASSWORD: "REPLACE_ME"
  # Optional: store the SimpleFIN access URL here instead of via claim_simplefin.
  SIMPLEFIN_ACCESS_URL: ""
