# AWS creds for S3 backups. Fill in, seal, apply:
#   kubeseal --format yaml < s3-secret.example.yaml > s3-sealed-secret.yaml
#   kubectl apply -f s3-sealed-secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: money-tracker-s3
  namespace: money-tracker
type: Opaque
stringData:
  aws_access_key_id: "REPLACE_ME"
  aws_secret_access_key: "REPLACE_ME"
